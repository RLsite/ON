# ON TracK Project Info

Version: `1.0.2`
Status: `GitHub connection tests now verify effective Contents and Pull Request write permissions with side-effect-free probes before Agent execution, and preserved plans are shown as execution retries rather than new approvals.`

## Goal
Build a web-first workspace where a chosen model can understand a project, connect to repositories, and perform approved actions in a controlled flow.

## Done
- Diagnosed the production approval failure in Agent Job `148fc546-...` against the exact token stored for `RLsite/ON`. The fine-grained PAT could read the repository (`200`) and the GitHub account had a push-capable repository role, but the create-ref endpoint returned `403` and explicitly required `contents=write`. The previous connection indicator confused account role with effective token permissions and therefore showed a healthy connection that could not execute. The forced GitHub connection test now performs two safe permission probes: creating an already-existing ref tests `Contents: write`, and opening a PR from a deliberately nonexistent head tests `Pull requests: write`; both can only return validation errors after authorization and cannot create repository state. Agent approval runs the same preflight before any branch is created, reports the exact missing permission, and preserves the plan. The connection modal shows read-only access in red, links to GitHub token settings, and validates a replacement token immediately after save. A preserved plan that already failed execution is labeled `Check permissions and retry` / `בדוק הרשאות ובצע שוב`, not as a new approval.
- Fixed the production failure found in Agent Job `342de7a7-...` after deploying `1.4.22`. NVIDIA completed translation, repository loading, checklist creation, and two fast reads (`newShell` and `authBtn`), proving that the timeout fix worked. It then requested the already-supplied `authBtn` read a second time, and the Worker failed the Job with `The model repeated the same repository read.` Repeated reads are now intercepted while accepting the model plan and again before GitHub execution for compatibility with already-saved Jobs. ON keeps the existing read results, logs that it prevented the duplicate, and moves the same Job to `finalize_reads`, where the model must return an answer or complete write/version/PR/deploy plan without another read. The Skill now states that `#authBtn` is inside `.newShell > .newMain > .newTop > .actions`, not `.newSidebar`, correcting the model's false location assumption without moving the icon on its behalf.
- Fixed the `1.4.21` regression found by inspecting the latest production Job (`9f2d616b-...`) in Cloudflare KV. The improved selector worked: NVIDIA found `#authBtn` in the first read and received the exact live `.newShell` markup. The failure happened afterward: the model emitted thousands of words of internal analysis instead of the Agent JSON, timed out repeatedly at 60 seconds, then exhausted the separate format-repair call. NVIDIA NIM Agent requests now use both provider-supported controls, the `/no_think` system directive and `chat_template_kwargs.enable_thinking=false`, with deterministic temperature, so the model plans without returning a long reasoning monologue. The system prompt no longer embeds the full Skill inside repository JSON; it sends compact metadata plus a bounded operational Skill excerpt. Query ranges use 8 surrounding lines instead of 14, read/repair payloads are capped at 30,000 characters, previous malformed output is capped at 1,600 characters in repair, and post-read/repair generations are reduced to 2,200/1,800 tokens. This keeps the same model, repository access, approval contract, and model-owned patch generation while fitting each continuation comfortably inside the Worker request window.
- Fixed the misleading request refresh behavior. Active Jobs now use Stop, terminal Jobs with saved reads and remaining allowance show `Continue from here`, and only the explicitly named `Retry from beginning` control creates a new Job. Continuing preserves the same Job id, original/translated request, repository excerpts, duplicate-read fingerprints, and read-round count. History hydration derives continuation availability from the persisted Job, so eligible older conversations gain the control after deployment. The read allowance is now four bounded rounds, `github.read_file.query` is explicitly one literal substring, results expose ranked `matchLocations` and independent suggestions, and the project Skill tells the model to locate the runtime `RL` account badge through `id="authBtn"` instead of searching 86 unrelated `RL`/`URL` matches.
- Enforced the Agent's no-false-completion contract in the Worker rather than relying on prompt text alone. An `answer` that claims it added, changed, moved, updated, fixed, removed, created, or deployed a repository change without a real `github.apply_patch`/`github.write_file` action is now rejected and sent through the existing contract-repair stage. If the model repeats the unsupported claim, the Job fails clearly instead of showing `complete`. A GitHub remote audit for the reported RL-icon test confirmed there was no Agent branch or commit and the live branch had not changed, so ON itself does not move the icon in this release; the NVIDIA Agent must produce and execute the change as the test requires.
- Fixed the approval sequence that produced `Resource not accessible by personal access token` followed by `This agent plan has expired`. The Worker no longer deletes an approval plan before GitHub execution succeeds. A failed write returns the Job to `waiting_approval`, keeps the same plan for 24 hours, and can recover a missing plan key from the same user's matching `jobId`/`planId`. The UI submits both ids, keeps the approval card active, and explains the required Fine-grained permissions (`Contents: Read and write`, `Pull requests: Read and write`) or Classic `repo` scope, allowing the user to replace the token and click approval again without rebuilding the request.
- Fixed Agent context selection so repository files and `AGENT_SKILL.md` are read from the configured live deployment branch (`claude/github-site-integration-fbb693`) instead of silently falling back to a stale GitHub default branch.
- Hardened the Agent read round-trip for Nemotron: JSON extraction now tolerates surrounding text, read results are included in the repair prompt, repeated read narration is not shown as a final answer, and plain model replies after a successful read are returned to the user instead of being replaced by the stale "I will read..." message. Model response content arrays are also normalized, while temporary capacity errors receive a clear retryable message.
- Removed the opening hero card so the workspace focuses on project selection, model requests, and the message history. Reversed the chat history so the newest message is at the top and the oldest is at the bottom, with an `x` delete action on every message.
- Removed seeded demo projects, libraries, chat messages, and starter project state. New accounts now start empty, while a one-time Worker migration removes only the known old demo records and preserves user-created records.
- Reworked the Projects panel into a compact workflow: a small `+` button opens the create editor, clicking a project name opens it, and each project row has a `⋯` menu with edit, rename, and delete actions. The editor reuses the existing per-user workspace endpoints, supports Hebrew/English labels, and stays closed until an action is chosen.
- GitHub repository seeded
- Clean web shell started
- Top actions added
- Left rail added
- Hebrew and English support started
- GitHub connection flow: live test button that calls the real GitHub API (repo reachability + token identity), with a true connected/failed status shown in the modal, the header dot, and the MVP readiness chip
- Fixed a bug where every popup dialog (GitHub, settings, model, help, preview, API, new request) failed to appear when opened from the new home screen. Root cause: all dialogs lived inside the legacy `.oldApp` container, which is hidden with `display:none` while the new home screen is being built — a hidden ancestor hides its children no matter what CSS state the dialog itself is in. Fix: on page load, the dialogs are moved to be direct children of `<body>` so they render as normal overlays regardless of the legacy shell's visibility. Verified with a headless-browser click test on each button (GitHub, model, settings, help) confirming the corresponding dialog opens, is visible on screen, and closes correctly.
- Reorganized the GitHub modal into three tabs — "חיבור GitHub", "תיקיית קוד מקומית" (local code folder for model context), and "דפדפן מקומי — כתובת האתר לבדיקה" — instead of one long scroll. Added a real "choose folder" experience: since browsers never expose actual OS paths from an `<input type=file>` picker, added a server-side `/api/fs/browse` endpoint (lists real subdirectories of any path, with Windows drive listing when no path is given yet) and a shared folder-picker dialog UI that lets the user click through real folders on disk and writes the chosen absolute path back into the local-folder and local-server-directory fields.
- Top-bar buttons (settings, help, GitHub, model, language) are now icon-only (with a title tooltip for the label) instead of icon+text, to match a cleaner look. Added a small LED dot on the GitHub and model buttons — green when actually connected, red when not — reusing the existing real-connectivity checks (`/api/github/status`, selected model) that already drove the older header/MVP indicators.
- Merged the `assets/` folder into the repo root: `assets/index.html` → `index.html` and `assets/server.js` → `server.js`, so the app shell and the local server both live in one place instead of being split across the root and a subfolder. Removed the old root `index.html` (a redirect stub for GitHub Pages) since the real app now sits at the root directly. Updated `package.json` (`main`, `start`), `scripts/build.js` (copies from `index.html` instead of `assets/index.html`), and `README.md` accordingly. `wrangler.jsonc` was left untouched — it still only publishes `dist/`, and its Worker `name` stays `on`.
- Rebuilt the language toggle from scratch as one system instead of the two partial, disconnected ones that used to leave stray English on screen. One `#newLangBtn` button, one `I18N` dictionary (130+ keys) driving `data-i18n` / `data-i18n-html` / `data-i18n-title` / `data-i18n-placeholder` across the home screen and all 8 relocated modals, hoisted to the top of the script so JS-generated content (the model-preset dropdown, the help-panel bullet lists) can read the current language too. Verified with a body-text sweep in a real browser: zero Latin-script leftovers in English mode, exact text restored on toggling back to Hebrew.
- Added the RL logo as the site favicon — resized to 64×64 and inlined as base64 to keep it self-contained in the single deployed HTML file.
- Added a clickable version number (Quick Status → Version) that re-fetches the page with the cache bypassed, compares the served `appVersion` against the running one, and offers a one-click reload when they differ.
- Gave the deployed site a real backend for the GitHub connection. Until now `on.rlapp.net` was pure static hosting (Cloudflare serving `dist/` with no server-side code at all), so every `/api/github/*` call 404'd and the whole GitHub modal was non-functional in production even though it worked locally against `server.js`. Added `worker.js` (a Cloudflare Worker `fetch` handler porting the five GitHub endpoints — config read/write, live status check, list issues, create issue — with the same logic as `server.js`) and wired it into `wrangler.jsonc` via `main` + an `ASSETS` binding, so the Worker runs for `/api/github/*` and falls through to the static build for everything else. Config (owner/repo/token) is stored in a KV namespace (`GH_CONFIG` binding) instead of a local JSON file, since Workers have no filesystem. The tool's other local-only features (folder browsing, starting a local dev server, driving a local Chrome for previews) are not and cannot be ported this way — they need real filesystem/process access a Worker structurally doesn't have, so they stay local-only by design.
- Fixed two related bugs found while building the above: (1) `updateConnDots()` called a `fetchJsonSafe` helper that only existed inside a different section's closure — the resulting `ReferenceError` was silently caught and read as "not connected," so the GitHub header LED showed red even when actually connected. Moved `fetchJsonSafe` to the top of the script so every section can reach it. (2) The visible "connect GitHub" button (top-bar icon, and the Settings shortcut) only opened the modal — it never loaded the saved owner/repo/enabled state into the form, so after a page refresh the fields always looked empty even though the config was safely persisted. Added a shared `openGithubModal()` that loads the saved config and refreshes the live status every time the modal opens, and wired every entry point to it.
- Created and bound the `GH_CONFIG` KV namespace in Cloudflare, then verified the whole GitHub connection end-to-end on the live site with a real repo (`RLsite/ON`) and a real Personal Access Token — connected status, read+write scope detection, and the header LED all confirmed correct in production, not just locally.
- A real test run caught a UX gap immediately: a real user typed an email into the Owner field and a full URL into the Repo field, which produced a confusing "Not Found" with no clue why. Reworded both labels to rule out the mistake explicitly, replaced the generic placeholders with this project's own real values (`RLsite` / `ON`), and added a hint line beneath the fields spelling out the breakdown from a full GitHub URL.
- Extended `worker.js` to also handle `/api/models/*` (list, select, add, update, delete, test) — same reasoning as GitHub: this only existed in `server.js` before, so it 404'd on the deployed site. Reuses the existing `GH_CONFIG` KV namespace under a different key (`models_config`), so no second namespace had to be created. Found and fixed the identical bug class as GitHub's: the real "Connect Model" entry points (top-bar icon, Settings shortcut, hero "Connect model" button) only opened the modal without loading/rendering the saved model list — only the dead legacy `#modelBtn` had that logic. Added `openModelModal()` and wired every entry point to it. Verified end-to-end with `wrangler dev --local`: add/select/test/delete all persist correctly through KV, and a full page reload correctly restores the previously-selected model.
- Added the RL logo as the visible sidebar brand mark (replacing the generic "O"), reusing the favicon's own data URI via JS rather than embedding the image twice.
- Removed a paragraph of leftover QA-tool copy from the model modal ("המודל שנבחר כאן הוא המודל שמבצע את בדיקות ה-QA...") — this app isn't the QA dashboard it was forked from.
- Added a "favorites" flow for models: deleting an external model now archives its full config server-side (label, provider, base URL, model id, and the API key) into a capped `recent` list (last 5) instead of discarding it. A "חוברו לאחרונה" section appears in the model modal whenever that list is non-empty, with a one-click "🔌 חבר מחדש" that restores the entry into the active list (fresh id) and selects it — no retyping the key. Verified end-to-end: add → delete → confirm it appears under recent with its key intact (never exposed to the client) → reconnect → confirm it's back in the active list, selected, and recent is empty again.
- Added real project management to the `Projects` panel: open, create, rename, and delete actions now persist in the workspace state and update the selected project immediately.
- **Real per-user login.** Until now, GitHub config and model config lived in one global KV key each — shared by every single visitor to `on.rlapp.net`. Anyone who connected a token or an API key made it usable (and overwritable) by anyone else who opened the site; a second person on a second computer would just see the first person's connection. Added Google Sign-In (OAuth authorization-code flow, `/api/auth/login|callback|logout|me`, session cookie backed by a KV-stored session record with a 30-day TTL) and rekeyed all storage by the logged-in user's Google id: `github_config:<uid>` and `models_config:<uid>` instead of flat `config`/`models_config` keys (also gives a provider-prefixed naming scheme for whenever a second repo-hosting provider is added later). Every GitHub/model/chat endpoint now 401s without a valid session. The whole app sits behind a login gate (logo, one-line description, "Sign in with Google") — logging in reveals the real app, logging out brings the gate back immediately. Requires a Google Cloud OAuth Client (Client ID in `wrangler.jsonc` as a plain var; Client Secret set directly as a Cloudflare Worker Secret, never committed). Verified end-to-end with `wrangler dev --local` using two simulated sessions: each user's GitHub config and model list are fully invisible to the other, one user's save never affects the other's data, and logout immediately invalidates the session server-side (not just cookie-side). The actual Google consent-screen click-through can only be verified on the live domain, since the registered redirect URI is `https://on.rlapp.net/api/auth/callback` — Google won't redirect to a localhost dev server.
- Connected the home-screen chat composer, which previously had no JavaScript wired to its send button at all (not even a failed network call). New `/api/chat` endpoint sends the typed prompt to the user's currently-selected external model (reusing the same provider-calling code as the connection-test ping) and the reply renders as a real chat exchange in the panel; a built-in (Claude) selection returns an explanatory message instead of attempting a call that has nothing to authenticate with from a Worker.
- The project-management actions (open/create/rename/delete, added to the home screen's Projects panel) only worked locally — they called `/api/workspace/*`, which existed in `server.js` but was never ported to `worker.js`, so every click 404'd on the deployed site (same bug class as GitHub/models before those were ported). Added the missing `/api/workspace`, `/api/workspace/select`, `/api/workspace/projects`, `/api/workspace/projects/update`, `/api/workspace/projects/open`, `/api/workspace/projects/delete` endpoints to `worker.js`, per-user (`workspace_config:<uid>` in the same KV namespace), behind the same login gate as everything else. Also fixed two bugs found in the same review: (1) three of the four new button labels (open/rename/delete project) had Hebrew text that didn't match the i18n dictionary, so toggling language and back would silently change them; (2) project/library names were written into `innerHTML` without escaping, unlike every other user-supplied string rendered this way elsewhere in the file — a stored-XSS gap (a project named e.g. `<img src=x onerror=...>` would have executed). Verified end-to-end with `wrangler dev --local`: create/rename/open/delete round-trip correctly, two simulated users' project lists stay fully isolated, a non-string `name` in the request body returns a clean 400 instead of crashing the handler, and a malicious project name renders as inert text, not markup.

## Recent Updates
- **Fixed the real "I do not have repository access" response after switching models.** Production KV confirmed the selected model was `nvidia/nemotron-3.5-lightning-30b-a3b`, while the stored Agent consent still belonged to `nvidia/nemotron-3-ultra-550b-a55b`. That separation is required for private-data safety, but ON previously sent the request anyway with repository access disabled, leaving the model to explain the missing access. Selecting, reconnecting, adding, or materially editing a model now immediately asks for explicit consent naming that model. Sending a chat also runs the same preflight, and the Worker returns `AGENT_CONSENT_REQUIRED` without contacting the provider or writing chat history if GitHub is configured but consent does not match. Consent is now bound to the internal model id, provider, provider model id, API base URL, and required repository/file scopes, so editing an existing connection to another destination cannot silently inherit approval.
- **Added a real, persistent `Stop model` control.** Active Agent Jobs now show a bilingual stop button. It immediately aborts the browser continuation, stores a user-scoped cancellation marker and terminal `canceled` Job state in Worker KV, removes a pending approval plan, and prevents a provider response that arrives after the click from overwriting the cancellation. Canceled Jobs stay stopped after refresh, show the exact stopped checklist/log state, and can be restarted only as a new request. Approval also refuses a plan whose Job was canceled.
- **Made English translation the first Agent stage.** Hebrew requests are no longer sent directly into the repository-planning prompt. The first bounded `/api/chat/continue` call translates the request faithfully into English, preserves filenames/URLs/identifiers, stores the translation in the resumable Job, displays it in the step log, and only then advances the checklist to planning. All technical planning and read-result stages use the persisted English request, while replies and model-generated checklist text are explicitly required to remain in Hebrew for the user. Requests already written in English complete this stage locally without an extra provider call.
- **Fixed the live "Wait for approval and execute" stall after inspecting the actual persisted Jobs in production KV.** The newest Job was not waiting for approval at all: it had no `planId` or public write plan, was still in `analyze_reads`, and NVIDIA had timed out three times while receiving 30,893 characters from two repository reads. The UI looked stuck because the checklist advanced mechanically after each read until its final approval item became active. Read completion no longer advances the checklist into approval; only creation of a real approval-gated plan does that. Hebrew requests also keep a Hebrew checklist when the model ignores the language instruction. Read payloads are reduced from 14,000 to 6,000 characters per result, query context is narrower, and giant data-URI/minified lines are compacted instead of consuming the whole budget. Provider calls now stop after 60 seconds with at most two separate transient attempts. When a real `planId` exists, the approval card gets an accented border, a larger approval button, and scrolls into view automatically.
- **Eliminated the structural source of repeated HTTP 524 failures.** The old `/api/chat` kept one Cloudflare request open while it could run up to six sequential model generations plus GitHub reads and JSON repairs. It now returns `202` immediately after storing a per-user Agent Job. The browser then calls `/api/chat/continue` serially; each continuation performs at most one model-provider call and persists the next state before returning. Every Job includes a model-generated checklist and a visible append-only step log, resumes automatically after a page refresh, and preserves the original request, file-read results, duplicate-read fingerprints, retry counters, pending approval plan, and exact next state for another runner. Provider calls time out before Cloudflare's proxy timeout, and capacity/timeouts retry in a new HTTP request instead of extending the current one.
- **Fixed the structural cause of Agent no-op responses on large files.** `github.read_file` previously returned only the first 50,000 characters, so the model could not see the active chat/UI code located later in the 220,000+ character `index.html`; it then edited the hidden legacy shell, narrated work without proposing an action, or stalled. Reads now support a focused `query` or a validated 1-400 line range, include total line/character metadata, and return an explicit first/last overview when an unfocused file is too large. The Agent can make up to two distinct read rounds, exact duplicate reads are stopped, and reaching the limit produces a clear no-change result instead of looping or pretending the request was completed.
- Extended the Agent contract end to end: code-change plans must include `github.update_version`, `github.create_pull_request`, and `github.deploy`. Approved execution updates the canonical version files, creates a PR from an `ontrack/agent-*` branch, and merges it into `claude/github-site-integration-fbb693`. Correction (see the entry below): this merge is a GitHub-only, git-level operation — it does not by itself update the running Cloudflare Worker. `on.rlapp.net` only picks up the change once a person separately runs `npm run deploy`.
- Added `github.apply_patch` to the central Agent validation allow-list so approved small file changes are no longer rejected after planning.
- Fixed Agent context loss after a repository read: the original current request is now included in the follow-up model call. Natural-language file-read intentions mentioning real repository paths are converted into safe read plans instead of being marked complete without execution. Previous chat history is not sent automatically without a separate user authorization.
- Added a `Refresh request` button to active, completed, and failed chat responses. It cancels an in-flight request before retrying it, prevents stale responses from overwriting the new attempt, and supports retrying saved text messages after a refresh.
- Clarified Agent plans: read-only `github.read_file` and `github.list_files` actions now show that no approval is needed, while `github.write_file`, `github.apply_patch`, and Pull Request actions show the approval flow. The read-results follow-up also gets one JSON repair attempt before the request is considered incomplete.
- Improved Agent reliability for NVIDIA NIM: the Worker loads the repository `AGENT_SKILL.md`, requires a strict JSON action contract, retries one malformed response, and rejects narration without executing it. Existing files can be proposed as small `github.apply_patch` diffs, avoiding full-file output limits; all writes remain approval-gated on a new branch.
- Added a visible chat-processing indicator: requests show sending, model processing, longer-running work, completion, errors, or an action plan waiting for approval. The indicator is available in Hebrew and English and does not claim a response is complete until the server returns it.
- Added per-Google-user Agent consent. The server stores the approved model, scopes, timestamp, and account-bound permission under a user-scoped key; changing models requires approval again.
- Activated the selected-model Agent flow: authorized requests receive repository context, read actions can return file context, and write plans require approval before a new branch and Pull Request.
- Added per-user chat history in Worker KV, restored after refresh, with an intentional clear-conversation action.
- Added a folder-selection button to library creation/editing, including a browser fallback when the live site cannot inspect the local filesystem.
- Added a `Skill folder` library type. On the live site it stores a selected folder reference; actual local file reading still requires a local Agent.
- Changed the model connection list to compact cards: three models per row on desktop, two on tablets, and one on phones.
- Fixed the RTL top-bar alignment so the action icons sit on the physical right side.
- Removed the visible Settings icon from the top bar as requested.
- Added typed library management with source icons and edit, rename, and delete actions.
- Fixed the chat layout so the message panel uses the full remaining width when the Projects sidebar is resized.
- Removed the visible Workspace title and placed the action icons in the top bar space.
- Added optional image attachments for the home chat and multimodal model requests.
- Added GitHub-token protection for model connections and model connection editing.
- Model deletion is now permanent and no longer archives the deleted connection for display.
- **Fixed a critical Agent bug found in review, live on production:** a `github.write_file`/`github.apply_patch` action's own `branch` field was only checked for valid syntax, not restricted to the freshly created `ontrack/agent-*` branch — a plan could set it to `claude/github-site-integration-fbb693` (the live-serving branch) and, on a single "Approve and run" click, write directly to it with no Pull Request, no merge step, and no visibility into which branch was targeted (the approval card never showed it). Fixed by making every write always target the auto-created change branch, full stop — the field is no longer read at execution time. Also added a denylist rejecting any write to `.github/*` (workflow files, which could grant CI-level code execution) at both plan-validation time and again defensively at execution time.
- **Fixed the root cause of "the model doesn't update GitHub files":** a plan that was valid JSON but broke a contract rule (most commonly a code change missing one of `github.update_version`/`github.create_pull_request`/`github.deploy`) was silently dropped — the user saw only the model's own reply text, with no approval card and no error, because the specific validation error was computed but never surfaced and the `agentAccessRequired` flag it set was never read by the frontend. The model now gets one repair attempt with the exact rule it broke, and if it still fails, the actual reason is now shown to the user instead of a silent no-op.
- **Closed the approval-transparency gap:** the approval card previously showed only a tool name, file path, and a character count for a pending write — never the actual content or diff — so a human could approve a real GitHub write (and, bundled with `github.deploy`, a merge into the live branch) without ever seeing what it contained. The plan sent to the browser now includes a real content/diff preview (up to 8,000 characters, with a truncation note beyond that), rendered in the chat as a scrollable, monospace block.
- **Fixed the "deploy" naming, which overstated what actually happens:** `github.deploy` only merges the approved Pull Request on GitHub — nothing in the repo calls Cloudflare or `wrangler`, so the live site is not updated by this action. The agent's own system prompt, `AGENT_SKILL.md`, and the execution result now say so explicitly, so neither the model nor the UI implies the live site changed until a person separately runs `npm run deploy`.
- Fixed a pre-existing i18n mismatch: the Settings-panel display/theme button's Hebrew markup ("תצוגה") didn't match its `titleTheme` dictionary entry ("מצב בהיר / כהה"), which is what it actually does (toggle light/dark mode) — corrected the markup to match.
- Added a copy-to-clipboard button to every chat bubble (user and assistant), next to the existing delete button.
- Fixed a version-consistency bug in the fix above: bumping the app version only in this file doesn't change what the app displays — `index.html` has its own `const appVersion` plus two hardcoded markup defaults, and `package.json`/`package-lock.json` have their own copies. All four now stay in sync with this file's `Version:` line on every release.
- **Fixed the actual root cause of a real "the model doesn't update GitHub files" report.** The Agent's repair calls (both the JSON-shape repair and the contract-violation repair added earlier) used a smaller token budget (1,600) than the original attempt (2,600), even though a code-change repair has to re-emit the same full patch/content plus a reply plus up to four bundled actions — not smaller than the original, sometimes larger. At 1,600 tokens the corrected JSON likely got cut off mid-object, which the parser can't recover from, so the repair silently produced nothing and the request fell back to displaying the original, now-stale error as if no repair had been attempted. Both repair calls now get 2,600 tokens, the validation-repair prompt restates the full four-action bundle instead of only the single rule that failed, and the final error message now says plainly when a repair reply couldn't be parsed at all.
- Added a single automatic retry for a transient model-capacity error (HTTP 429/503/"rate limit"/"overloaded"), found from a real user hitting this while the fix above was being tested. Adversarial review found the first version of this retry could compound: one `/api/chat` request can chain up to 6 sequential model calls, and each could independently retry-and-wait, worst case ~7.2s of added delay plus up to 6 duplicated full generations on an already slow, non-streamed response. Fixed by sharing one retry budget across all calls in a single request, so at most one retry happens per request regardless of how many calls hit capacity errors. Also reworded the "still working" hint (previously blamed only "files or GitHub actions," misleading for a plain question hitting a hidden retry) and the capacity error message (now says explicitly when the one retry was already spent, instead of telling the user to do what the server just tried).
- A real approved write plan reached execution (the model produced a complete plan, the approval card showed the actual diff, the user clicked "Approve and run") and failed with GitHub's own `Resource not accessible by personal access token` error — but the message gave no way to tell which of the ~5 distinct GitHub calls in a write plan (reading the base branch, creating the change branch, each file write, the version-file updates, opening the Pull Request, merging it) produced it, and each of those needs a different token permission. Every stage now wraps its GitHub call and prefixes any failure with which stage it was ("Failed while creating the Pull Request: ..."), so a permissions problem points at the exact step — and therefore the exact scope to add to the connected GitHub token. (Root cause that time turned out to be an expired token, not a missing scope — the stage label is what let the user find that themselves.)
- A real Agent plan (approved, with a real diff shown in the preview) turned out to patch `index.html`'s hidden legacy `.oldApp` shell (`display:none`, never visible to any user) instead of the live `.newShell` UI it looks nearly identical to — a syntactically valid patch that would have passed every check, merged cleanly, and changed nothing a user could see. `AGENT_SKILL.md` (fetched and given to the model as context on every request) now explicitly names both shells, says which one is live, and requires confirming which container an element belongs to before proposing a change to `index.html` — matching class names alone (both have a `.railBrand`/`.brandMark`) isn't enough to tell them apart.
- After the fix above, the model correctly read `.newShell`'s brand-mark CSS before proposing anything — then announced the request was finished with no approval card ever shown and no write, PR, or merge ever made. Confirmed against the real repository (GitHub API: zero Pull Requests exist, open or closed). It described the change in a plain-text `reply` instead of proposing it as an `actions` entry — a distinct failure from "the model doesn't update files" (that one was a silently rejected plan; this one never became a plan at all). Both the live system prompt and `AGENT_SKILL.md` now explicitly forbid saying a change is done, made, applied, or complete unless the action that makes it is actually present in that same response.
- Re-audited per-user data isolation end to end, prompted by a direct question about it rather than a bug report — every KV resource added since the original global-key incident (workspace projects/libraries, agent consent, agent plans, chat history) plus every endpoint that touches them. Code-level adversarial review across 6 areas found the isolation intact everywhere, with one low-severity hygiene gap: two fully-implemented but completely unwired chat handlers (`handleLegacyChat`, `handlePlainChat`) that read/wrote per-user KV exactly like the live handler but sat outside the router's auth gate — not reachable today, but a real risk if ever wired in carelessly later. Removed both. Backed the code review with live two-real-user attack testing: seeded a real pending write-plan (with fake secret content) under one user's `agent_plan:<uid>:<planId>` key and confirmed the other user couldn't approve/execute it even with the real planId (rejected as expired — the lookup requires the caller's own uid, not just the planId); confirmed a user attempting to delete another user's chat message by its real ID silently no-ops instead of succeeding; confirmed concurrent writes by two users to every resource type never crossed into each other's data.
- Deep-dive into "why does it still not work" after the resumable-jobs rework: verified live=repo=1.4.23 (not a stale deploy), the frontend continue-loop drives correctly, reasoning mode is already disabled for NVIDIA (`/no_think` + `enable_thinking:false`), and the state machine is sound — the remaining killer was arithmetic: every provider call was hard-aborted at 60 seconds, and `AGENT_JOB_MAX_TRANSIENT_FAILURES = 2` permanently failed the job after just two such aborts. A 550B-parameter model on NVIDIA's shared endpoint routinely needs more than 60s to emit a full 2,600-token plan with zero streaming (no bytes until it finishes), so two consecutive slow responses — routine under load — killed every substantive request in ~2 minutes. Raised the per-call timeout to 85s (each `/api/chat/continue` request makes exactly one provider call, so the only hard ceiling is Cloudflare's ~100s empty-response window, previously observed as a real 524) and the transient-failure allowance to 4, with the existing growing backoff between attempts.
- A real connection attempt with Gemini failed with Google's own "models/gemini-2.0-flash-lite is no longer available" — Google retired the whole gemini-1.5/2.0 family, and the app's preset dropdown listed exactly those retired models. Verified the current lineup against Google's live model documentation and updated the presets (`gemini-3.6-flash` as the fast default, `gemini-3.5-flash-lite` as cheapest, `gemini-2.5-flash`/`gemini-2.5-pro` still stable); the `generateContent` endpoint itself is still supported, only the model ids died. Also added a call-time mapping in `worker.js` from each retired id to its current successor, so connections users saved before the retirement keep working with zero manual editing (`server.js`'s four legacy local-only Gemini call sites were deliberately left untouched).

## In progress
1. Make the home screen fully product-like
2. Build real project and library data
3. Google OAuth needs a live click-through test on the deployed site (setup steps done: OAuth Client created, Client ID wired in, Client Secret needs to be set as a Cloudflare Worker Secret)

## Next
1. Add a project picker
2. Extend the repository connection flow with real actions (branches, commits, PRs) beyond issues
3. Add model execution approval
4. Add controlled actions on GitHub

## Notes
- Production is web-first on `https://on.rlapp.net/`; GitHub changes are not live until the Cloudflare Worker is deployed from the live branch.
- Keep the UI calm and simple
- Remove any remaining QA-specific leftovers as they appear
